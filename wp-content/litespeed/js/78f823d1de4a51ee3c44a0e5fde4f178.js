(function(){'use strict';const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const nav=document.querySelector('.nav');if(nav){const onScroll=()=>nav.classList.toggle('scrolled',window.scrollY>20);onScroll();window.addEventListener('scroll',onScroll,{passive:!0})}
const mb=document.querySelector('.menu-btn');if(mb){const drawer=document.createElement('div');drawer.className='m-drawer';drawer.innerHTML='<div class="m-scrim"></div><nav class="m-panel" aria-label="Menu di động"></nav>';const panel=drawer.querySelector('.m-panel');const links=document.querySelector('.nav-links');if(links){links.querySelectorAll(':scope > a').forEach(a=>{const c=a.cloneNode(!0);c.className=a.classList.contains('active')?'m-link active':'m-link';panel.appendChild(c)});const drop=links.querySelector('.nav-drop');if(drop){const head=document.createElement('div');head.className='m-group-title';const btn=drop.querySelector('button');head.textContent=btn?btn.textContent.trim():'Dịch vụ';panel.appendChild(head);drop.querySelectorAll('.dropdown a').forEach(a=>{const c=a.cloneNode(!0);c.className=a.classList.contains('active')?'m-sub active':'m-sub';panel.appendChild(c)})}}
const cta=document.querySelector('.nav-right .btn');if(cta){const c=cta.cloneNode(!0);c.className='btn btn-primary m-cta';panel.appendChild(c)}
const lang=document.querySelector('.lang');if(lang){const row=document.createElement('div');row.className='m-lang';row.appendChild(lang.cloneNode(!0));panel.appendChild(row)}
document.body.appendChild(drawer);const close=()=>{drawer.classList.remove('open');document.body.style.overflow=''};const open=()=>{drawer.classList.add('open');document.body.style.overflow='hidden'};mb.addEventListener('click',open);drawer.querySelector('.m-scrim').addEventListener('click',close);panel.addEventListener('click',e=>{if(e.target.closest('a'))close();});document.addEventListener('keydown',e=>{if(e.key==='Escape')close();})}
document.querySelectorAll('.lang button').forEach(b=>{b.addEventListener('click',()=>{document.querySelectorAll('.lang button').forEach(x=>x.classList.remove('on'));b.classList.add('on')})});const reveals=document.querySelectorAll('.reveal');if(reduce)reveals.forEach(r=>r.classList.add('in'));else{const io=new IntersectionObserver((es)=>{es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target)}})},{threshold:0.12,rootMargin:'0px 0px -7% 0px'});reveals.forEach(r=>io.observe(r))}
function fmt(v,dec){return dec?v.toFixed(dec):Math.round(v).toLocaleString('en-US')}
function run(el){const target=parseFloat(el.dataset.count);const dec=parseInt(el.dataset.dec||'0',10);const pre=el.dataset.prefix||'',suf=el.dataset.suffix||'';const dur=1600,t0=performance.now();(function tick(now){const p=Math.min((now-t0)/dur,1);const e=1-Math.pow(1-p,3);el.innerHTML=pre+fmt(target*e,dec)+suf;if(p<1)requestAnimationFrame(tick);else el.innerHTML=pre+fmt(target,dec)+suf})(t0)}
const counters=document.querySelectorAll('[data-count]');if(reduce)counters.forEach(el=>{el.innerHTML=(el.dataset.prefix||'')+fmt(parseFloat(el.dataset.count),parseInt(el.dataset.dec||'0',10))+(el.dataset.suffix||'')});else{const cio=new IntersectionObserver((es)=>{es.forEach(e=>{if(e.isIntersecting){run(e.target);cio.unobserve(e.target)}})},{threshold:0.6});counters.forEach(el=>cio.observe(el))}
if(!reduce){const cw=document.querySelector('.circle-wrap');if(cw){window.addEventListener('pointermove',(e)=>{const dx=e.clientX/window.innerWidth-0.5;const dy=e.clientY/window.innerHeight-0.5;cw.style.transform=`translate(${dx * 18}px, ${dy * 18}px)`;document.querySelectorAll('.node').forEach((n,i)=>{const m=(i+1)*10;n.style.transform=`translate(${dx * m}px, ${dy * m}px)`})},{passive:!0});document.addEventListener('pointerleave',()=>{cw.style.transform='';document.querySelectorAll('.node').forEach(n=>n.style.transform='')});const N=5;for(let i=0;i<N;i++){const s=document.createElement('div');s.className='spark';const ang=(i/N)*Math.PI*2;const rad=38+Math.random()*6;s.style.left=(50+Math.cos(ang)*rad)+'%';s.style.top=(50+Math.sin(ang)*rad)+'%';s.style.animation=`bob ${4 + Math.random() * 3}s ease-in-out ${Math.random() * 2}s infinite`;cw.appendChild(s)}}}
const tabBar=document.querySelector('.svc-tabs');if(tabBar){const tabs=[...tabBar.querySelectorAll('a[href^="#"]')];const targets=tabs.map(a=>{const el=document.querySelector(a.getAttribute('href'));return el?(el.classList.contains('anchor')?(el.parentElement||el):el):null});const barH=tabBar.getBoundingClientRect().height||60;const spy=()=>{const line=window.scrollY+barH+90;let idx=0;targets.forEach((t,i)=>{if(!t)return;const top=t.getBoundingClientRect().top+window.scrollY;if(top<=line)idx=i});tabs.forEach((t,i)=>t.classList.toggle('active',i===idx))};spy();window.addEventListener('scroll',spy,{passive:!0});window.addEventListener('resize',spy,{passive:!0})}
const canvas=document.getElementById('constellation');if(canvas){const ctx=canvas.getContext('2d');let W,H,DPR,nodes=[],mouse={x:-9999,y:-9999,tx:-9999,ty:-9999};let raf=null,running=!0;const COUNT=()=>{const area=W*H;return Math.max(34,Math.min(96,Math.round(area/19000)))};function resize(){DPR=Math.min(window.devicePixelRatio||1,2);W=canvas.clientWidth;H=canvas.clientHeight;canvas.width=W*DPR;canvas.height=H*DPR;ctx.setTransform(DPR,0,0,DPR,0,0);buildNodes()}
function buildNodes(){const n=COUNT();nodes=[];for(let i=0;i<n;i++){nodes.push({x:Math.random()*W,y:Math.random()*H,vx:(Math.random()-0.5)*0.28,vy:(Math.random()-0.5)*0.28,r:Math.random()*1.6+0.8,})}}
const LINK=140;function frame(){if(!running)return;ctx.clearRect(0,0,W,H);mouse.x+=(mouse.tx-mouse.x)*0.08;mouse.y+=(mouse.ty-mouse.y)*0.08;for(const p of nodes){p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>W)p.vx*=-1;if(p.y<0||p.y>H)p.vy*=-1;const dx=p.x-mouse.x,dy=p.y-mouse.y;const d2=dx*dx+dy*dy;if(d2<14000&&d2>0.01){const f=(14000-d2)/14000*0.9;const d=Math.sqrt(d2);p.x+=(dx/d)*f;p.y+=(dy/d)*f}}
for(let i=0;i<nodes.length;i++){const a=nodes[i];for(let j=i+1;j<nodes.length;j++){const b=nodes[j];const dx=a.x-b.x,dy=a.y-b.y;const dist=Math.sqrt(dx*dx+dy*dy);if(dist<LINK){const al=(1-dist/LINK)*0.5;ctx.strokeStyle=`rgba(129,140,248,${al})`;ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke()}}
const mdx=a.x-mouse.x,mdy=a.y-mouse.y;const mdist=Math.sqrt(mdx*mdx+mdy*mdy);if(mdist<LINK+50){const al=(1-mdist/(LINK+50))*0.7;ctx.strokeStyle=`rgba(192,132,252,${al})`;ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(mouse.x,mouse.y);ctx.stroke()}}
for(const p of nodes){ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fillStyle='rgba(199,202,255,0.85)';ctx.fill()}
raf=requestAnimationFrame(frame)}
window.addEventListener('pointermove',(e)=>{const rect=canvas.getBoundingClientRect();if(e.clientY<rect.bottom){mouse.tx=e.clientX-rect.left;mouse.ty=e.clientY-rect.top}});window.addEventListener('pointerleave',()=>{mouse.tx=-9999;mouse.ty=-9999});const heroIO=new IntersectionObserver((entries)=>{entries.forEach(e=>{const wasRunning=running;running=e.isIntersecting;if(running&&!wasRunning&&!reduce){cancelAnimationFrame(raf);frame()}})},{threshold:0});window.addEventListener('resize',()=>{resize()});resize();if(reduce){running=!0;frame();running=!1;cancelAnimationFrame(raf)}else{frame();heroIO.observe(canvas)}}})()
;